import { Link, useNavigate } from 'react-router-dom'
import { useAuth } from '../contexts/AuthContext'

const Navbar = () => {
    // TODO: Student to implement Navbar with auth state
    return <nav>Task Management System</nav>;
    
};

export default Navbar;